services:
  - type: web
    name: graph-theory-app
    env: python
    buildCommand: ./render-build.sh
    startCommand: cd backend && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: NODE_VERSION
        value: 20.0.0
